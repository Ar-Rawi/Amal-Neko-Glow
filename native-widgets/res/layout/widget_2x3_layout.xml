<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_widget_glass"
    android:orientation="vertical"
    android:padding="0dp">

    <!-- Header: Title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Amal Neko"
        android:textColor="#ffffff"
        android:textSize="13sp"
        android:textStyle="bold" />

    <!-- Sub-header Row: Badge + Add Button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="4dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/widget_pill_bg"
            android:paddingStart="6dp"
            android:paddingTop="2dp"
            android:paddingEnd="6dp"
            android:paddingBottom="2dp"
            android:text="2x3 Goals"
            android:textColor="#ff75c3"
            android:textSize="9sp"
            android:textStyle="bold" />

        <!-- Filter Button -->
        <ImageView
            android:id="@+id/widget_filter_btn"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="8dp"
            android:src="@drawable/ic_filter"
            android:padding="4dp"
            android:contentDescription="Filter categories" />

        <!-- + Add Button -->
        <ImageView
            android:id="@+id/widget_add_btn"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/ic_add"
            android:padding="4dp"
            android:contentDescription="Add task" />
    </LinearLayout>

    <!-- Scrollable Task ListView -->
    <ListView
        android:id="@+id/widget_task_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="6dp"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:scrollbars="none"
        android:fadingEdge="vertical"
        android:fadingEdgeLength="12dp" />

    <!-- Empty State View -->
    <TextView
        android:id="@+id/widget_empty_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="6dp"
        android:gravity="center"
        android:text="No active tasks.\nTap here to add one!"
        android:textColor="#9ca3af"
        android:textSize="12sp"
        android:visibility="gone" />

    <!-- Footer -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="By Rawi"
        android:textColor="#818cf8"
        android:textSize="9sp" />
</LinearLayout>
